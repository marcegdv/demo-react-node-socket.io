const Messages = (props) => {
    const { a } = props;
    return (
        <div style={styles.container}>

        </div>
    );
};

export default Messages;

const styles = {
    container: {
        backgroundColor: '#222',
        color: '#ddd',
        padding: '8px',
        borderRadius: '8px',
        borderWidth: '2px',
        borderStyle: 'solid',
        borderColor: '#ddd',
    },
};